[project]
name = "ddf-hoax-temporal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "altair",
    "google-auth",
    "marimo==0.24.0",
    "numpy",
    "polars",
    "python-dotenv",
    "requests",
    "sastrawi",
    "scikit-learn",
    "urllib3",
]

[tool.marimo.runtime]
# The full reviewed-claim explorer exceeds marimo's 8 MB default during HTML export.
output_max_bytes = 40000000
